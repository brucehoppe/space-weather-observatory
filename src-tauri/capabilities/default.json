{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege capability set. The UI can save an export through a native dialog and open a verified external link in the system browser; it has no general file, shell or HTTP access. All network acquisition happens in the Rust backend.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "core:window:allow-start-dragging",
    "dialog:allow-save",
    "opener:default"
  ]
}
